#include<stdio.h>
int main()
{
	int length1,breadth1,length2,breadth2;
	printf("Enter value for length1 and breadth1:");
	scanf("%d %d",&length1,&breadth1);
	int area1=length1*2;
	printf("Area1=%d",area);
	
	printf("Enter value for length2 and breadth2:");
	scanf("%d %d",&length2,&breadth2);
	int area1=length1*length2;
	printf("Area1=%d",area);
}
